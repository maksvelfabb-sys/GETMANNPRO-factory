import streamlit as st
import pandas as pd
from modules.database import load_data

def show_admin_panel():
    st.header("👥 Керування системою")
    
    tab1, tab2, tab3 = st.tabs(["Користувачі", "Статистика", "Технічний стан"])
    
    with tab1:
        st.subheader("Редагування доступу")
        st.info("Функція додавання/видалення користувачів буде доступна в наступному білді.")
        
    with tab2:
        df = load_data()
        st.metric("Всього замовлень у базі", len(df))
        st.write("Останні замовлення:")
        st.dataframe(df.tail(5))
        
    with tab3:
        st.success("З'єднання з Google Drive: Активне ✅")
        st.success("Доступ до папки креслень: Підключено ✅")
